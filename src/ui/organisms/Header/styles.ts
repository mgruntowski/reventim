import styled from "styled-components";
import spacings from "styles/spacings";

export const _Header = styled.header`
  max-width: 1600px;
  box-sizing: border-box;
  margin: auto;

  padding: ${spacings.x8} 0;
`;
